apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aqconnecta

resources:
  - deployment.yaml

# Service, Ingress e ConfigMap do frontend ficam no repo do backend:
# ../AQConnecta-back/k8s/frontend/
