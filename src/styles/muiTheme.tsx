import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#5E63B6',
      light: '#aba9bb',
      dark: '#464555',
    },
  },
});

export default theme;
